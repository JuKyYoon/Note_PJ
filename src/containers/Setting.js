import React from 'react';
import BigText from '../components/BigText';

const Setting = () => {
    return (
        <div>
           <BigText>set</BigText>
        </div>
    );
};

export default Setting;
