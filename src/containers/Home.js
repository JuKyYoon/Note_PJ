import React from 'react';
import BigText from '../components/BigText';
import Neditor from '../components/neditor'

const Home = () => {
    return (
        <div>
            <Neditor/>
        </div>

    );
};

export default Home;
